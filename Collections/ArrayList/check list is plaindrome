public class Set {
public static void main(String[] args) {
//	List<Integer> l = new LinkedList<>(Arrays.asList(1,2,3,2,1));
//	boolean pali = true;
//	for(int i=0; i<l.size()/2;i++) {
//		if(!l.get(i).equals(l.get(l.size()-1-i))) {
//			pali=false;
//			break;
//		}
//	}
//	System.out.print(pali);
	
	List<Integer> l = new LinkedList<>(Arrays.asList(1,2,3,2,1));
	Stack<Integer> st = new Stack<>();
	boolean pali =true;
	for(int num:l) {
		st.push(num);
	}
	for(int nums:l) {
		if(nums != st.pop()) {
			pali = false;
			break;
		}
	}
	System.out.print(pali);
}
}
